server:
  port: 20001
# Zero MQ Configuration
zeromq:
  address-port: tcp://*:20002
# MQTT Sender Details
mqtt:
  broker: mosquitto
  password: guest
  port: 1883
  topic: coreData
  user-name: guest
spring:
  cloud:
    consul:
      discovery:
        health-check-path: /api/v1/ping
        health-check-interval: 30s
